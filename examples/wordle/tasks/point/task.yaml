# Task: instructions, the one Environment it runs in, and the Verifiers that score it.
name: point
version: 0.1.0
instructions: instruction.md
environment: wordle
verifiers:
  - solved
# The hidden word. It is State, so the Agent never sees it.
initial_state:
  secret: point
