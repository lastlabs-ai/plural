# Agent: runs the keyword Harness. The model is recorded but never called,
# so this Agent needs no credentials.
name: scripted
version: 0.1.0
model: openai/gpt-5.6-luna
harness: scripted-triage
auth_mode: none
